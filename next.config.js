/** @type {import('next').NextConfig} */
const nextConfig = {
  // Make sure experimental features aren't interfering
  experimental: {
    // serverActions: true, // This should be enabled by default in Next.js 14
  },
  // Check if you have any custom fetch configurations
  // ...
}

module.exports = nextConfig 